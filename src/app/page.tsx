import styles from './page.module.css'

const Home = () => {
  return (
    <main className={ styles.main }>
      <div>
        Hi! Follow the links above.
      </div>
    </main>
  )
}

export default Home
